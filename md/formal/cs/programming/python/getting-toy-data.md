{{yaml}}

### sklearn

```python
import sklearn
dataset = sklearn.datasets.load_iris(as_frame=True)
df = dataset.frame
```

* `load_iris()`
* `load_diabetes()`
* `load_digits()`
* `load_linnerud()`
* `load_wine()`
* `load_breast_cancer()`
